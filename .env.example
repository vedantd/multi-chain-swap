# Application (optional)
# NEXT_PUBLIC_APP_NAME=multi-chain-swap

# RPC and API keys (add when integrating providers)
# Solana RPC (optional; falls back to mainnet public endpoint if unset)
# NEXT_PUBLIC_SOLANA_RPC_URL=
# NEXT_PUBLIC_RELAY_API_KEY=
# DEBRIDGE_API_KEY=
