# ============================================================================
# Multi-Chain Swap - Environment Variables
# ============================================================================
# Copy this file to .env.local and update with your values.
# All variables are OPTIONAL and have sensible defaults.
# See docs/ENVIRONMENT_VARIABLES.md for complete documentation.
# ============================================================================

# ============================================================================
# Application Configuration
# ============================================================================

# Application name (currently unused, reserved for future use)
# NEXT_PUBLIC_APP_NAME=multi-chain-swap

# ============================================================================
# Solana RPC Configuration
# ============================================================================

# Solana RPC endpoint for wallet connections and blockchain queries.
# Default: Public Solana RPC (https://api.mainnet-beta.solana.com)
# WARNING: Public RPC may return 403 errors under high load.
# RECOMMENDED: Use a dedicated RPC provider (Alchemy, QuickNode, etc.)
# 
# Example (Alchemy):
# NEXT_PUBLIC_SOLANA_RPC_URL=https://solana-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_API_KEY
#
# Example (QuickNode):
# NEXT_PUBLIC_SOLANA_RPC_URL=https://your-endpoint.solana-mainnet.quiknode.pro/YOUR_KEY/
#
# SECURITY NOTE: This variable is exposed to the client browser.
# Do not include sensitive API keys unless using public endpoints.
NEXT_PUBLIC_SOLANA_RPC_URL=

# ============================================================================
# Bridge Provider API Configuration
# ============================================================================

# Relay API base URL
# Default: https://api.relay.link
# Only change if using custom Relay endpoint or testing environment
RELAY_API_URL=https://api.relay.link

# deBridge DLN API base URL
# Default: https://api.dln.trade
# Only change if using custom deBridge endpoint or testing environment
DEBRIDGE_API_URL=https://api.dln.trade

# ============================================================================
# Fee Sponsorship Configuration
# ============================================================================

# Solana address that pays transaction fees and rent for deposit transactions.
# This address must have sufficient SOL to cover transaction costs.
#
# Fallback order:
# 1. params.depositFeePayer (from API request)
# 2. RELAY_DEPOSIT_FEE_PAYER (this variable)
# 3. SPONSOR_SOLANA_ADDRESS (alias, see below)
# 4. DEFAULT_DEPOSIT_FEE_PAYER (hardcoded constant)
#
# IMPORTANT: Fee Sponsorship Requirements
# - Full fee sponsorship (covering destination chain fees via subsidizeFees
#   and subsidizeRent) requires Enterprise Partnership with Relay.
# - Until Enterprise Partnership is obtained, users pay their own Solana
#   transaction fees (depositFeePayer is typically set to user's address).
# - Once Enterprise Partnership is obtained:
#   - Set this to sponsor address or omit to use DEFAULT_DEPOSIT_FEE_PAYER
#   - Add subsidizeFees: true and subsidizeRent: true to quote requests
#   - Add x-api-key header with Enterprise API key
#
# See: https://docs.relay.link/features/fee-sponsorship
#
# Example:
# RELAY_DEPOSIT_FEE_PAYER=YourSolanaAddressHere111111111111111111111111111
RELAY_DEPOSIT_FEE_PAYER=

# Alias for RELAY_DEPOSIT_FEE_PAYER (same fallback chain)
# Use whichever naming convention you prefer
SPONSOR_SOLANA_ADDRESS=

# ============================================================================
# Relay Quote Parameters
# ============================================================================

# Referrer identifier for monitoring transactions from a specific source.
# Useful for analytics and tracking integration usage.
# This value is automatically included in all Relay quote requests.
#
# See: https://docs.relay.link/bridging-integration-guide#quote-parameters
#
# Example:
# RELAY_REFERRER=my-integration-v1.0
RELAY_REFERRER=

# ============================================================================
# Advanced Relay Features
# ============================================================================
#
# The following Relay quote parameters can be set programmatically via API:
# - slippageTolerance: Slippage in basis points (e.g., "50" for 0.5%)
# - refundTo: Address to send refunds to in case of failure
# - topupGas: Enable gas top-up on destination chain (boolean)
# - topupGasAmount: Gas top-up amount in USD decimal format (e.g., "2000000" = $2)
# - useExternalLiquidity: Use canonical+ bridging for more liquidity (boolean)
# - appFees: Array of app fees for monetization (see Relay docs)
#
# These parameters are passed through the /api/quotes endpoint and forwarded
# to Relay's quote API. See src/types/swap.ts for the SwapParams interface.
#
# See: https://docs.relay.link/bridging-integration-guide
# ============================================================================

# ============================================================================
# Logging Configuration
# ============================================================================

# File path where quote evaluation logs are written.
# Logs are written in JSONL format (one JSON object per line).
# Default: ./logs/quotes.jsonl
#
# Ensure the directory exists and the application has write permissions.
# The log file is created automatically if it doesn't exist.
#
# Example:
# QUOTE_ACCOUNTING_LOG_PATH=./logs/quotes.jsonl
#
# For production, consider using an absolute path:
# QUOTE_ACCOUNTING_LOG_PATH=/var/log/multi-chain-swap/quotes.jsonl
QUOTE_ACCOUNTING_LOG_PATH=

# ============================================================================
# Development Notes
# ============================================================================
#
# 1. Copy this file: cp .env.example .env.local
# 2. Edit .env.local with your values (this file is gitignored)
# 3. Restart dev server after changing variables
# 4. Never commit .env.local or .env files
#
# For production deployment:
# - Vercel: Set variables in project settings
# - Docker: Use --env-file flag or docker-compose
# - Self-hosted: Set in process manager (PM2, systemd, etc.)
#
# See docs/ENVIRONMENT_VARIABLES.md for complete documentation.
# See docs/DEPLOYMENT.md for deployment instructions.
# ============================================================================
